# Online Book Store

This is a simple Node.js application used for demonstrating CI/CD pipeline using Jenkins, Docker, and Kubernetes.

## Features
- Displays list of books
- Runs on port 3000

## Run locally
npm install
node server.js